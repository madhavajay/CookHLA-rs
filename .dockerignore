# Keep build contexts small. The cookhla-rs image bundles repos/CookHLA/1000G_REF (the reference
# panels), so it is NOT ignored here; only build artifacts + the beagle-rs Java history are.
target/
fixtures/
**/.git/
repos/beagle-rs/beagle/
repos/beagle-rs/reference/
repos/beagle-rs/target/
